Below is a step-by-step beginner guide to build a secure login system with JWT authentication. If you follow it exactly, you will end up with a working authentication project you can reuse in other apps.

We will use:

Node.js

Express

JSON Web Token

bcrypt

Estimated time: 30–60 minutes

Step 1 — Create the Project Folder

Create a folder:

auth-practice

Inside it create:

auth-practice
│
├── server.js
├── users.json
└── public
    ├── login.html
    ├── register.html
    ├── dashboard.html
    └── script.js
Step 2 — Initialize the Project

Open a terminal inside the folder and run:

npm init -y

Install required packages:

npm install express jsonwebtoken bcryptjs cors

What they do:

Package	Purpose
express	creates the web server
jsonwebtoken	generates login tokens
bcryptjs	securely hashes passwords
cors	allows frontend requests
Step 3 — Create the Backend Server

Open server.js

const express = require("express");
const jwt = require("jsonwebtoken");
const bcrypt = require("bcryptjs");
const fs = require("fs");
const cors = require("cors");

const app = express();
app.use(express.json());
app.use(cors());

const SECRET = "supersecretkey";
Step 4 — Load the User Database

We will store users in a simple JSON file.

Add:

function getUsers() {
  const data = fs.readFileSync("users.json");
  return JSON.parse(data);
}

function saveUsers(users) {
  fs.writeFileSync("users.json", JSON.stringify(users, null, 2));
}

Create users.json

[]
Step 5 — Create Register Route

Add this to server.js

app.post("/register", async (req, res) => {
  const { username, password } = req.body;

  const users = getUsers();

  const hashedPassword = await bcrypt.hash(password, 10);

  users.push({
    username,
    password: hashedPassword
  });

  saveUsers(users);

  res.send("User registered");
});

What happens:

User submits password
↓
Password is hashed
↓
Hashed password stored
Step 6 — Create Login Route

Add this:

app.post("/login", async (req, res) => {
  const { username, password } = req.body;

  const users = getUsers();
  const user = users.find(u => u.username === username);

  if (!user) {
    return res.status(401).send("Invalid credentials");
  }

  const valid = await bcrypt.compare(password, user.password);

  if (!valid) {
    return res.status(401).send("Invalid credentials");
  }

  const token = jwt.sign(
    { username },
    SECRET,
    { expiresIn: "1h" }
  );

  res.json({ token });
});

This creates a JWT token when login succeeds.

Step 7 — Create Protected Route

Add:

function authenticate(req, res, next) {

  const header = req.headers["authorization"];

  if (!header) return res.status(401).send("Access denied");

  const token = header.split(" ")[1];

  try {
    const verified = jwt.verify(token, SECRET);
    req.user = verified;
    next();
  } catch {
    res.status(401).send("Invalid token");
  }
}

Protected endpoint:

app.get("/dashboard", authenticate, (req, res) => {
  res.send(`Welcome ${req.user.username}`);
});
Step 8 — Start the Server

Add to bottom of server.js

app.listen(3000, () => {
  console.log("Server running on port 3000");
});

Run it:

node server.js
Step 9 — Create Register Page

public/register.html

<h2>Register</h2>

<input id="username" placeholder="Username">
<input id="password" type="password" placeholder="Password">

<button onclick="register()">Register</button>

<script src="script.js"></script>
What is this?
Step 10 — Create Login Page

public/login.html

<h2>Login</h2>

<input id="username" placeholder="Username">
<input id="password" type="password">

<button onclick="login()">Login</button>

<script src="script.js"></script>
What is this?
Step 11 — Create Dashboard

public/dashboard.html

<h1>Secure Dashboard</h1>

<button onclick="loadDashboard()">Load Data</button>

<p id="result"></p>

<script src="script.js"></script>
What is this?
Step 12 — Create Frontend Script

public/script.js

async function register() {

  const username = document.getElementById("username").value;
  const password = document.getElementById("password").value;

  await fetch("http://localhost:3000/register", {
    method: "POST",
    headers: {"Content-Type":"application/json"},
    body: JSON.stringify({username,password})
  });

  alert("Registered!");
}
Login function

Add:

async function login() {

  const username = document.getElementById("username").value;
  const password = document.getElementById("password").value;

  const res = await fetch("http://localhost:3000/login", {
    method:"POST",
    headers:{"Content-Type":"application/json"},
    body: JSON.stringify({username,password})
  });

  const data = await res.json();

  localStorage.setItem("token", data.token);

  window.location = "dashboard.html";
}
Dashboard access

Add:

async function loadDashboard() {

  const token = localStorage.getItem("token");

  const res = await fetch("http://localhost:3000/dashboard", {
    headers:{
      Authorization: "Bearer " + token
    }
  });

  const text = await res.text();

  document.getElementById("result").innerText = text;
}
Step 13 — Test the System

1️⃣ Start server

node server.js

2️⃣ Open:

register.html

Create user.

3️⃣ Open:

login.html

Login.

4️⃣ Go to:

dashboard.html

Press Load Data.

You should see:

Welcome username
What You Just Built

You implemented:

✔ password hashing
✔ login authentication
✔ JWT tokens
✔ protected routes
✔ frontend login system

This is the same core authentication flow used in real web applications.

If You Want a Much Better Version

Next upgrades I can show you:

role-based access control (RBAC)

refresh tokens

SQL database login

secure cookies

login rate limiting

session expiration

Those turn this into a production-level authentication system.